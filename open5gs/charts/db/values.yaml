# Default values for db

image:
  registry: docker.io
  repository: mongo
  tag: "4.4"
  pullPolicy: IfNotPresent

services:
  db:
    type: NodePort
    nodePort: 30007

config: {}

storage:
  volumeClaimTemplate:
    spec:
      resources:
        requests:
          storage: 1Gi
      accessModes:
        - ReadWriteOnce

global:
  containerPorts:
    db: 27017
